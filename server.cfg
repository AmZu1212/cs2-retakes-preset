echo ---------------------------
echo - Loading server settings -
echo ---------------------------

// Server Config - Retakes

// Retakes - Features:
//
//		- RTV to change the map, only competitive map pool for now
//
//		- Full Retakes with grenades, and weapon changing
//
// 		- For fully working skins system, you have to set up your own MySQL local server.
//        edit "addons\counterstrikesharp\configs\plugins\WeaponPaints\WeaponPaints.json" in order to connect to it. need to update username and password
//
//		- If you want awp spawns or different settings, edit the config in "addons\counterstrikesharp\plugins\RetakesAllocator\config\config.json"
//
// 		- max players should be set in launch flags (using "-maxplayers N")




// Your Server name, feel free to put whatever!
hostname "▲▼ 🗡[IL]⚡Zubi's Retakes ⚡!skins⚡NO AWP⚡128T⚡"


// Show online
sv_lan "0"

// Set region (255 is worldwide)
sv_region "255"


// Admin Settings:
// sv_password "---"								// Protect your server with a password
// rcon_password "---"								// Allow users to run RCON console commands. Do not enable unless you know what you're doing.
// sv_downloadurl "---"								// Server FastDL (Should not end with a /) - If you have a FastDL server


// Performance
sv_maxrate "0"											// Max bandwidth rate allowed on server, 0 == unlimited
sv_minrate "0"											// Min bandwidth rate allowed on server, 0 == unlimited
sv_parallel_sendsnapshot "1"							// Enables multi-threading.
sv_clockcorrection_msecs "15"							// The server tries to keep each players m_nTickBase withing this many msecs of the server absolute tickcount
sv_hibernate_when_empty "0"								// Puts the server into extremely low CPU usage mode when no clients connected


// Fun Commands: (surf/bhop)
// sv_cheats "false"
// sv_accelerate "5.15"									// Used to increase max speed
// sv_airaccelerate "12" 								// Used to increase strafe speed
// sv_cheats false										// Allow clients to use cheat commands
// host_timescale "1"									// Change the speed of time on your server.
// sv_autobunnyhopping "1"								// Enable bhopping


// Gamemode Settings:
// mp_ct_default_grenades ""         					// Spawn CTs without grenades
// mp_t_default_grenades ""         					// Spawn Ts without grenades
// mp_limitteams "0"		   							// Allow free team-switching
// bot_quota "0"        								// Disable bots
// Warmup Settings:
// mp_warmup_online_enabled "true"      				// Enable warmup on dedicated server
// mp_warmup_pausetimer "1"         					// Enable infinite warmuptime until disabled
// mp_warmup_start										// Start warmup
// mp_warmup_end										// ends warmup, keep this off
// mp_maxrounds 10										// doesn't work since the retake plugin takes over
// mp_friendlyfire 0									// activate/disable friendly fire. recommended to keep off.

// Chat Notifcations
//say >>> <your chat message here> <<<					// to speak in chat from console :)

echo ---------------------------
echo - Done loading server.cfg -
echo ---------------------------

